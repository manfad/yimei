<form id="contactForm" novalidate>
  <div class="field row2">
    <div class="field">
      <label>First name <span class="req">*</span></label>
      <input name="first" type="text" placeholder="Jane" />
      <div class="err-msg">Please enter your first name.</div>
    </div>
    <div class="field">
      <label>Last name <span class="req">*</span></label>
      <input name="last" type="text" placeholder="Tan" />
      <div class="err-msg">Please enter your last name.</div>
    </div>
  </div>
  <div class="field">
    <label>Company</label>
    <input name="company" type="text" placeholder="Your company or mill" />
  </div>
  <div class="field row2">
    <div class="field">
      <label>Email <span class="req">*</span></label>
      <input name="email" type="email" placeholder="jane@company.com" />
      <div class="err-msg">Please enter a valid email.</div>
    </div>
    <div class="field">
      <label>Phone</label>
      <input name="phone" type="tel" placeholder="+60 ..." />
    </div>
  </div>
  <div class="field">
    <label>What can we help with?</label>
    <select name="topic">
      <option>General enquiry</option>
      <option>Pre-treatment</option>
      <option>Biological treatment</option>
      <option>Filtration &amp; membrane</option>
      <option>Water recovery &amp; reuse</option>
      <option>Monitoring &amp; control</option>
      <option>Service &amp; maintenance</option>
    </select>
  </div>
  <div class="field">
    <label>Message <span class="req">*</span></label>
    <textarea name="message" placeholder="Tell us about your site, waste stream and what you'd like to achieve..."></textarea>
    <div class="err-msg">Please add a short message.</div>
  </div>
  <div class="form-foot">
    <button type="submit" class="btn">Send message <span class="arr">&rarr;</span></button>
    <span class="form-note">We'll reply within one business day.</span>
  </div>
</form>
<div class="form-success" id="formSuccess">
  <div class="sc">
    <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="#6FC23A" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M20 6 9 17l-5-5"></path>
    </svg>
  </div>
  <h3>Message sent</h3>
  <p>Thanks for reaching out. Our team will get back to you within one business day.</p>
  <button class="btn btn--ghost" type="button" id="formReset" style="margin-top:24px">Send another</button>
</div>

<script>
  const form = document.getElementById('contactForm') as HTMLFormElement;
  const success = document.getElementById('formSuccess');
  const resetBtn = document.getElementById('formReset');

  function resetForm() {
    form.reset();
    form.style.display = '';
    success?.classList.remove('show');
    form.querySelectorAll('.field').forEach((f) => f.classList.remove('bad'));
  }

  resetBtn?.addEventListener('click', resetForm);

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    let ok = true;

    function check(name: string, test: (v: string) => boolean) {
      const input = form.querySelector(`[name="${name}"]`) as HTMLInputElement;
      const field = input.closest('.field');
      const bad = !test(input.value.trim());
      field?.classList.toggle('bad', bad);
      if (bad) ok = false;
    }

    check('first', (v) => v.length > 0);
    check('last', (v) => v.length > 0);
    check('email', (v) => /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(v));
    check('message', (v) => v.length > 0);

    if (!ok) return;

    form.style.display = 'none';
    success?.classList.add('show');
  });

  form?.querySelectorAll('input, textarea').forEach((el) => {
    el.addEventListener('input', () => el.closest('.field')?.classList.remove('bad'));
  });
</script>
